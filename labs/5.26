#include <stdio.h>
#include <string.h>

int main(void) {

   char string[50];
   
   scanf("%[^\n]",string);
   
   for(int i=0;string[i] != '\0';i++)
      if(string[i] != ' ')
         printf("%c",string[i]);

   printf("\n");

   return 0;
}
