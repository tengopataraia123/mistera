#include <stdio.h>

int main(void) {

   /* Type your code here. */
   
   int nums[] = {0,0,0};
   int max = 0;
   
   scanf("%d %d %d",&nums[0],&nums[1],&nums[2]);
   
   for(int i=0;i<3;i+=1){
      if(nums[i]>max){
         max = nums[i];
      }
   }
   
   printf("%d\n",max);

   return 0;
}
