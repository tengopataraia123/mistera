#include <stdio.h>

int main(void) {
   
   int num;
   
   scanf("%d",&num);

   if(20<=num && num<=98){
      while(num%10 != num/10)
         printf("%d ",num--);
      printf("%d \n",num);
   }else{
      printf("Input must be 20-98\n");
   }

   return 0;
}
