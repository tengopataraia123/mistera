#include <stdio.h>
#include <string.h>

int main(void) {
   char userInput[50];
   char firstLetter;

   scanf("%s", userInput);
   scanf(" %c", &firstLetter);

   if (userInput[0] == firstLetter/* Your solution goes here  */) {
      printf("Found match: %c\n", firstLetter);
   }
   else {
      printf("No match: %c\n", firstLetter);
   }

   return 0;
}
