#include <stdio.h>

/* Your solution goes here  */
float PyramidVolume(double a,double b,double c){
   return a*b*c/3;
}

int main(void) {
   double userLength;
   double userWidth;
   double userHeight;

   scanf("%lf", &userLength);
   scanf("%lf", &userWidth);
   scanf("%lf", &userHeight);

   printf("Volume: %f\n", PyramidVolume(userLength, userWidth, userHeight) );

   return 0;
}
